---
title: fail-closed
type: definition
summary: "The host policy mapping an evaluation error to a deny plus an alarm, applied to facts. It never applies to theory drift or caller defects."
status: evolving
---

The host policy mapping an evaluation error to a deny plus an alarm, applied to facts. It never applies to theory drift or caller defects.
