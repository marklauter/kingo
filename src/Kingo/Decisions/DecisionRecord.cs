namespace Kingo.Decisions;

/// <summary>
/// The outcome of an authorization evaluation — the value Check returns and the audit event carries (CloudTrail-style).
/// Expected to identify the subject, the subject set asked about, the verdict, the snapshot evaluated at, and the caller.
/// Shape TBD — stub capturing the domain name ahead of the rewrite-interpreter work.
/// </summary>
public sealed class DecisionRecord;
